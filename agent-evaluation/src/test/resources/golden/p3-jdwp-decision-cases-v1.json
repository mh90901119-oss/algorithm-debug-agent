{
  "schemaVersion": "1.0",
  "fixtureId": "p3-jdwp-decision-cases-v1",
  "skill": {
    "name": "algorithm-debug",
    "version": "1.1"
  },
  "cases": [
    {
      "caseId": "reuse-sufficient-history",
      "given": "A prior same-context collection already answers the stack question and is SUCCESS, MATCHED and evidenceUsable.",
      "expectedDecision": "REUSE_EVIDENCE",
      "required": ["cite prior runId and collectionId", "do not rerun the UT"]
    },
    {
      "caseId": "request-minimal-jdwp",
      "given": "Static and CodePath evidence identify one method, but the value inside that method is missing.",
      "expectedDecision": "CREATE_THEN_EXECUTE_JDWP_PLAN",
      "required": ["use the Method Catalog Source Anchor", "state the evidence gap", "bound hits, frames, depth, bytes and timeout"]
    },
    {
      "caseId": "target-failure-remains-input",
      "given": "The target UT throws an algorithm exception after the Collector started and artifacts were archived.",
      "expectedDecision": "ANALYZE_TARGET_FAILURE",
      "required": ["separate target failure from Agent failure", "do not describe the Agent as crashed"]
    },
    {
      "caseId": "reject-unusable-collection",
      "given": "The collection is TIMED_OUT, TRUNCATED, source-drifted, or its baseline is CHANGED.",
      "expectedDecision": "DO_NOT_CONFIRM_ROOT_CAUSE",
      "required": ["label missing or contradictory evidence", "preserve the collection for diagnosis"]
    },
    {
      "caseId": "reject-unsupported-assumption",
      "given": "The question asks for a field projection or variable allowlist that Collector 1.0 does not support.",
      "expectedDecision": "REPORT_UNSUPPORTED_CAPABILITY",
      "required": ["do not claim the requested values were captured", "do not silently widen collection"]
    }
  ]
}
