# OpenCode adapter scaffold

OpenCode agents, skills, commands and TypeScript tools call the stable `ada` CLI. They do not run
Maven, Code Path Tracer or JDWP directly. Files here are placeholders until the CLI contracts exist.

