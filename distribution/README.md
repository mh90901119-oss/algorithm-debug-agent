# Distribution

Future release assembly for the `ada` CLI, Code Path Tracer Bundle, JDWP Collector, schemas,
configuration, license notices and checksums. Build output belongs under `distribution/build/`.

