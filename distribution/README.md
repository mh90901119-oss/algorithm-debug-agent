# Distribution

Future release assembly for the `ada` CLI, canonical Algorithm Debug Skill, OpenCode adapter,
Code Path Tracer Bundle, JDWP Collector, schemas, configuration, license notices and checksums.
The adapter is registered once with OpenCode and continues to load product assets from this
distribution; it does not copy the Skill into the global Skill directory. Build output belongs under
`distribution/build/`.
